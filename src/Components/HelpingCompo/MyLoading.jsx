import React from 'react';

const MyLoading = ({className}) => {
    return (
        <div className={`${className}`}>
            Loading...........
        </div>
    );
};

export default MyLoading;